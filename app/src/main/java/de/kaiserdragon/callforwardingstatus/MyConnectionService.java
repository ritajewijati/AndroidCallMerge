/*
package de.kaiserdragon.callforwardingstatus;
import android.telecom.ConnectionService;
import android.telecom.Conferenceable;
import android.telecom.Conference;
import android.telecom.PhoneAccountHandle;

public class MyConnectionService extends ConnectionService {
    @Override
    public Conference onCreateConference(Conferenceable conferenceable) {
        return new MyConference();
    }

    private static class MyConference extends Conference {
        public MyConference(PhoneAccountHandle phoneAccount) {
            super(phoneAccount);
        }

        @Override
        public void onMerge() {
            //if(fail)
            //else
        }
    }
}
*/
